# Force npm to install the sharp Linux x64 binary when building on Vercel.
# Without this, sharp installed on Windows will have the wrong native binary
# and will silently fail to load on Vercel's Linux runtime.
sharp_binary_host=https://npmmirror.com/mirrors/sharp
sharp_libvips_binary_host=https://npmmirror.com/mirrors/sharp-libvips
